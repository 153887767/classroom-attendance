import { DataTypes } from 'sequelize'

const STRING = DataTypes.STRING
const INTEGER = DataTypes.INTEGER
const BOOLEAN = DataTypes.BOOLEAN
const DATE = DataTypes.DATE

export { STRING, INTEGER, BOOLEAN, DATE }
